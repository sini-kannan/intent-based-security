apiVersion: v1
kind: ContainerIntent
metadata:
  name: nginx-app
spec:
  egress:
    - domains:
        - google.com
      ports:
        - number: 443
          protocol: TCP
  - service: mongodb
  - service: mongodb

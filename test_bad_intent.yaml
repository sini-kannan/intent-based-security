apiVersion: intent.security/v1alpha1
kind: ContainerIntent
metadata:
  name: "bad name with spaces"
spec:
  egress:
    - domains:
        - api.stripe.com
      ports:
        - number: 99999    # Invalid port
          protocol: TCP
    - ports:               # Missing domains
        - number: 80
          protocol: TCP

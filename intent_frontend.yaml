apiVersion: v1
kind: ContainerIntent
metadata:
  name: frontend-app
spec:
  egress:
    - domains: ["api-service"]
      ports:
        - number: 8080
          protocol: TCP
    - domains: ["cdnjs.cloudflare.com"]
      ports:
        - number: 443
          protocol: TCP
